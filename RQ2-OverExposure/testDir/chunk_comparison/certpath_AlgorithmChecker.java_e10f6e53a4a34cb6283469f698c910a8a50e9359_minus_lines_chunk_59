	Comparing:
---------Patch Chunk Lines-----------------
0         if (constraints instanceof DisabledAlgorithmConstraints) {
1            ((DisabledAlgorithmConstraints)constraints).permits(currSigAlg, cp);
2             // DisabledAlgorithmsConstraints does not check primitives, so key
-------------------------------------------
	is minus_lines  with
----------------Target Code--------------------
0        if (constraints instanceof DisabledAlgorithmConstraints) {
1            ((DisabledAlgorithmConstraints)constraints).permits(currSigAlg, cp);
2            // DisabledAlgorithmsConstraints does not check primitives, so key
---------------------------------------------
Differences:	[]
--	--	--	--	--
Match Status: minus_lines

--------------for ref---------------------
0         if (constraints instanceof DisabledAlgorithmConstraints) {
1            ((DisabledAlgorithmConstraints)constraints).permits(currSigAlg,
2                currSigAlgParams, cp);
3             // DisabledAlgorithmsConstraints does not check primitives, so key
--------------------------------------------
